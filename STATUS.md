# STATUS — project map (read me first)

One read to see the whole folder: what lives where, which docs are live, and where the work stands. CLAUDE.md (auto-loaded) is the deep guide to *how* systems work — this file is the *where*. Keep-current rules: CLAUDE.md → "Keeping STATUS.md current".

**Last updated:** 2026-08-02 (git tighten-up: repo re-homed here, single `main`, July 29–31 work committed + pushed both remotes · owner playtest round: fabricator freeze fix, Combat Sim feedback, walk-in Starwing boarding, stat descriptions · `Plans/Game-Brief.md` added for ideation/chat sessions)

## Where the work stands

- **ComputerKit v2 — procedural rebuild (2026-08-02, owner: "these assets look so grainy"):** the Rodin-sketch kit's noisy diffuses read as grain on clean hard surfaces, so all 8 `CK1_*` GLBs were rebuilt **procedurally** (`Assets/3D/ComputerKit/build_computerkit_v2.py`, single-piece rebuild mode; ComputerKit.blend now holds v2): crisp primitives, flat Starwing-livery palette (sRGB→linear converted), Cycles AO baked to COLOR_0 vertex colors, ZERO image textures — pieces dropped to 34–399 KB. Same filenames/dims/contracts so wiring was untouched except one kit.js line (enable vertexColors on COLOR_0 geometry, Mine/kit.js convention). Wall ends are flat squares (seamless ×3-tiled joins proven), roof is a true 6×6 square (2×2 joins clean), the rack's LED column moved to the camera-facing side. Rodin raws/ledger/v1 script kept on disk as provenance. **Rule reaffirmed the hard way: hard-surface = procedural Blender, Rodin only for organic/sculptural** (addendum §5 had it right). 360/360; rig-verified with in-game screenshots.

- **ComputerKit Era-1 art round (2026-08-02, owner: "use Art_prompt_generic + Rodin; data-centers are ORGANIZED"):** the Generation Engine's placeholder boxes are replaced by an **8-GLB `CK1_*` kit** (all <1.7 MB, `models/`) generated through the headless Rodin pipeline in the house style (Plans/Art_prompt_generic stem — owner added Rick and Morty to it this round — plus Starwing-livery white/gunmetal/teal and explicit organization language): wall panel (1 re-roll — v1 came back dark/rounded; v2 borrowed the WallDoor's proven wording), door panel (2.3×2.5 doorway **boolean-cut through** — Rodin's own openings aren't reliable, verified walkable against the collision gap), roof tile (octagon footprint — kept the procedural slab beneath so tiled corners never gap), the four interior machines (field terminal / 4-tower server centerpiece kept at TRUE scale with twin capsule collision / bench / rack with its teal LED column yawed to camera), and a delivery pallet that appears by the door while a schematic is part-fed. `zones/ComputerBuilding/kit.js` preloads with the procedural shell as fallback + portal-style late attach; **interior placement is now aligned rows/aisles** (per-chunk grids, uniform facing, jitter ≤0.15, aisles floored player-passable) per the owner's organization principle — random scatter is gone. `computerCore` fog rescaled to the room (homes preset was blacking it out). Source `Assets/3D/ComputerKit/` (ledger, raws incl. the wall v1, single-piece-rebuild build script, blend, check renders). 360/360; 13/13 rig checks. **New Blender trap logged:** `to_track_quat('-Z','Z')` is degenerate (arbitrary camera roll — verification renders lie in both directions); use a manual look-at with world +Z pinned. Rodin also returns arbitrary small leans now (13.7°/16.2° this batch) — the pipeline gained per-piece PCA snap-to-axis leveling (toggleable; misfires on open-frame pieces).

- **GENERATION ENGINE Round 1 — the buildable computer (2026-08-02, owner-directed via chat handoff):** the game's namesake machine is now a **player-built building** at the Landing Site. The player founds it anywhere valid by placing 6×6 **chunks** on the track grid (CORE menu tab → BUILD CHUNK/RECLAIM/MOVE DOOR modes; click or [E]; teal/red 6×6 cursor), the plan generates walls/roof/collision Labyrinth-style (`zones/ComputerBuilding/shell.js` — one geometry module feeds exterior AND the walk-in interior; the collision door-cut is deliberately wider than the visual gap), and the room inside (`computerCore` zone, walk-in from gen 1) fills with the machine as it evolves — 15→95% floor across Era 1's four generations (flight-cased field terminal → twin mission servers → integration bench → expedition rack, per the addendum's mission-grade fiction). Evolution = recompile-count eligibility (thresholds stubbed 0) + a material-delivery schematic on the CORE panel; evolving grants pending chunks. `ComputerSystem` (save v15 + migration), generation data table (`computerGenerations.js`) as single source of truth, valid-cell mask from the real keep-out data (`siteMask.js` — building's own shell circles flag-skipped so growth is never vetoed), door/nav-chip/path-ribbon all track the player's door choice. 360/360 tests (5 new suites incl. shell walk-probe + save migration); rig-verified 31/31 live checks (found + place + walk in/out + evolve + 2nd chunk + reload persistence + invalid probes + screenshots). Plan bug fixed en route (collision end-circles blocked the door); menu-backdrop fix (pointer events never reached the canvas with a panel open). Executed subagent-driven per `docs/superpowers/plans/2026-08-02-generation-engine-round1.md`. **Round-2+ hooks (NOT built):** Eras 2–5 kits + real ComputerKit art (placeholder toon boxes ship now), stacking, module-slot variants, color variants (owner dictates color semantics), mechanical bonuses, Recompile-terminal relocation. **Owner attention wanted:** buildable ground is scarce — every small tree/rock vetoes a full chunk square, so valid cells cluster at the meadow rim; consider letting chunk placement clear small flora (like it clears ground cover), or a smaller mask margin.

- **Owner playtest round (2026-07-31, four notes, all shipped + rig-verified, 342/342):** (1) **"Movement stuck after closing the fabricator window"** — the real defect was a game-wide FREEZE: `HUD._refreshCrafting` called a `_refreshCraftingWithQueue()` that didn't exist, the throw escaped the gameLoop callback, and THREE's `setAnimationLoop` never reschedules after a throw — the whole game died the moment the panel opened (only noticed on close). The method now exists (renders the queued-crafts line the +Q button always needed). (2) **Combat Sim feedback** — a live `COMBAT SIM ▸ STR/DEF %` chip rides the top-center HUD stack while the rig trains (`hud.combatSim`, indicator in `hud.update()`), and the player character now visibly drills at the rig: `player.trainingPose` (set per-frame in main.js within 3 units of the live rig) plays the Run clip slowly in place. (3) **Landing Site** — the watchtower at (−7,−6) that sat on the pad→mine tutorial path is gone (ZoneAssets row removed; `tower` model key kept). (4) **Starwing boarding is walk-activated** — `_addCaveEntrance` grew `opts { walkIn, triggerR }`: entering the radius fires the switch itself (armed only after the player is seen outside it, so spawns can't loop); `triggerR` 4.0 was tuned against rig screenshots up the bay axis to the point where the hull half-covers the walking player (fully-visible at dist 4.8, full x-ray ghost by 2.8). Plus: every stats-panel row now states what a level buys (`STAT_DESCS` in StatsSystem, numbers mirrored from the real consumers) — and that audit found **Dexterity and Perception have no gameplay consumers at all**; their rows say "Dormant subsystem — no effect yet" honestly, but they want a real effect or removal.

- **THE CINDERFORGE — 16th zone (2026-07-30, owner: "similar to labyrinth but volcano themed"):** the Meltwater Rift's **Ember Chasm is open** — the winch at (13, 10.5) now lowers into a volcanic forge-maze where the World Gates were cast (`_addSealedGate` swapped for the promised `_addCaveEntrance`; the return Ancient Gate lands back on the chasm apron via spawnOverride, verified to the decimal). Same one-map grammar as its Labyrinth sibling: `zones/Cinderforge/layout.js` holds a NEW 15×15 map (117 floor cells, distinct braiding) that generates wall slabs → ZONE_ASSETS rows, collision chains and the flood-fill test (`tests/systems/cinderforgeLayout.test.js`: connectivity, ≥16-cell anvil trek, slab/collision coverage, spawn cells). Route: entry court → Forgemaster plaza (basalt golem with lava-crack veins — only a statue, *for now*) → vent yard / crucible court → the Great Anvil sanctum (NW trek payoff: rune-dais anvil, spinning ember ring, twin gargoyles). Dead ends pay out ingot hoards beside standing lava pools (MeshBasic rim-over-core discs + rising ember motes on the `_spinners` update-fn convention); nodes are obsidian/embermoss/tungsten/copper/quartz, fauna cavecrab/scalerunner/duneplate on tight leashes, no boss. **13-GLB `Forge_*` kit** via the headless Rodin pipeline (16 jobs / 3 re-rolls, `Assets/3D/Cinderforge/build_cinderforge.py`, Cinderforge.blend watcher-parked): BOTH ArchGate rolls came back lying RINGS — so the kit leans in and ships the threshold as an upright half-buried ring, an *unfinished World Gate casting* (ring-specific normalize: width 4.2, aperture bottom sunk 0.18 — walk line clear, rim collision at ±1.25/±1.85); the column re-roll's stray base beam was trimmed to the top-slab footprint; the wall normalizes by LENGTH to 4.52 so the ×1.13 join rule holds. Blender gotcha logged: `ob.dimensions` is STALE right after `ob.data.transform()` — read vertex spans (shipped a 0.78-unit mini-wall before the fix). 342/342; verified live on the Playwright rig: all 13 GLBs loaded, 44 dressing placements + 169 slabs at exact authored coords, wall-vs-floor collision probes, "[E/ACT] Enter The Ember Chasm" prompt on the apron, and screenshots of entry/plaza/sanctum/crucible at the fixed camera. Design doc: `docs/superpowers/specs/2026-07-30-cinderforge-design.md`. *Follow-up hooks:* resident NPCs, a Forgemaster boss + chapter rung, orange-glow pass on the stele runes (Rodin returned silver twice).

- **Labyrinth outer walk (2026-07-30, owner: "Add to labyrinth layout"):** the maze grew 15×15 → 19×19 (`zones/Labyrinth/layout.js`) — a perimeter ring corridor now wraps the whole maze, split by cross-walls at c8 into two arcs that never meet. The **east arc** is a loop: south opening east of the entry court → east opening into the NE room → north opening behind the tomb alcove. The **west arc** is *the long dark*: one way in off the west corridor, around the SW corner (dead-end stub, chest) and all the way up to a **hidden back door into the shrine sanctum's west arm**, a gargoyle watching outside. The grid grew symmetrically (2 cells/side) so every old cell keeps its world position — `CENTER` now derives from `SIZE` in layout.js and the test; labyrinth got a `ZONE_BOUNDS` entry (±48). New on the ring: rich quartz (NW corner) + silica nodes, a cavecrab and scalerunner on tight ring leashes, tomb chests at the SW stub and NE corner, steles/braziers signposting every opening, and 4 new point lights — 11 zone lights + the player lamp fill the 12-light bucket EXACTLY, so no shader-program recompile (one more light in this zone tips it to the 24 bucket). 337/337; verified live on the Playwright rig (both openings, the NW corner, the back door — note for future rigs: hard-snap `sceneManager.camera` on teleport, headless SwiftShader runs too few frames for the camera lerp to settle).

- **Snake-river refine (2026-07-30, owner: "Refine river"):** the river read as ground fog — the ribbon's wide body→ground feather (right for worn trails) is wrong for water — and its tails/hairpin apexes overshot the ground plane (x ±42..46 vs bounds ±40), floating lit ribbon over the void. Now: pathStrip grew an opt-in `bankShade` water mode (9 columns — near-full-strength body to 0.80, a thin dark wet-bank rim at 0.92 derived from the LOCAL ground color so it rides the teal→gold phase, rim fades with the tip taper); tails end exactly at the plane edge (x 40, taperFrac 0.012 ≈ a 5-unit dim into the world's dark) and apexes pulled on-plane (±37/37.5), with the barrier chain WIDENING through the bends (r 2.6 → 3.3 past |x| 28) so the ±39 clamp strip stays sealed — the topology BFS test still proves every band pocket bridge-only. Bend dressing rolls denser (the bends are visible ground now), sparks 44 → 62. 337/337; before/after verified at 7 spots on the Playwright rig.

- **Owner batch round (2026-07-29, five asks, all shipped + verified in-game, 337/337):** (1) **"Instead of multiple rivers, make it a snakey river"** — the Maw's four full-width rivers are ONE serpentine now (`RIVER_PATH` in canopy.js): the four corridor crossings keep their exact sine courses (every pad/bridge/spire/keep-out held), joined by hairpin bends in the flank jungle (apexes on-plane since the 2026-07-30 refine, sealed by the bend-widened barrier chain) — a grid-BFS test now *proves* every band pocket is bridge-only instead of asserting chain shapes. One ribbon draws the whole course (pathStrip grew an opt-in per-row `colorAt` — body/core/ground grade teal→jade→gold continuously along the run), collision/banks/sparks/keep-outs all ride the same polyline. (2) **THE LABYRINTH — 15th zone** — the ancient-stone-maze kit is wired: an Ancient World Gate stands half-drowned in Atlantis' south end-chamber pool (the "back door" foreshadowing, paid off) opening on a 15×15-cell maze (`zones/Labyrinth/layout.js`, one ASCII map generates wall slabs → ZONE_ASSETS rows, collision chains, and the test flood-fill; walls are reveal-shaded slab RUNS along cell centerlines, cross-slabs at turns, columns as pillars). Entry court → braided corridors → Minotaur plaza (statue, braziers, bull-skull trophy, hex ArchGate thresholds) → fountain court/well yard → shrine sanctum (glow orb + spinning ring + gargoyles), tomb chests + a sprung spike pit in the dead ends; cavecrab/scalerunner/duneplate prowl on tight leashes; quartz/silver/silica/obsidian nodes; deliberately no boss — the Minotaur is a statue, *for now*. `_addPortal` grew an optional `spawnOverride` so the return gate lands in the end chamber, not Atlantis' north gate. `tests/systems/labyrinthLayout.test.js` guards connectivity, shrine-trek length, slab/collision coverage, and spawn cells. (3) **Two ambient NPCs via the full headless Rodin pipeline** (`Assets/3D/Labyrinth/build_npcs.py` → `Npc_Warden`/`Npc_Delver.glb`, breathing-spine Idle rigs): the Warden, a hooded tortoise-folk keeper by the gate; the Delver, a mouse-folk explorer camped at the fountain — both first-roll text-to-3D successes on the proven face formula, verified animating in-game. NOTE: job submission now runs headless over stdlib urllib (create/status/download with the addon's free-trial key — `rodin_npc_jobs.py` pattern in the session scratchpad, ledgers beside the assets); the character raws stood Z-up already, the slab test laid them face-down (robe front = "flat base"), so characters skip it. (4) **The Starwing** — the owner's delta-wing concept image replaces the dropship (`build_starwing.py` → `Landing_Starwing.glb`, image-to-3D). **v2 next morning (owner: "short and flat like a hamburger... VTOL C130 mixed with a bullet train — trust the source material — park it on its belly"):** round 1 came back span == length and read as a pancake; the re-roll passes an elongated `bbox_condition` ([5,8,2]) AND the build now *enforces* the concept's proportions per-axis (26 long × 16 span × 6.4 high) instead of trusting Rodin, derives orientation numerically (thickness = smallest span; belly = the broader outer slab; the taller length-end is the fin tail) instead of eyeballing renders, and cuts the cargo bay as a C-130-style rear tunnel up the CENTER of the upswept aft — its mouth opens right onto the landing pad, so landing → walking into the hold is one straight line. Hull collision chains track the measured planform (needle nose ±0.7 → wing band ±8 → twin booms ±3.2), tree keep-outs run the full 26-unit spine + bay approach, and the boarding prompt ("Enter the Starwing") fires inside the lit tunnel. (5) **"I'm starting to notice time between changing scenes"** — first-visit switches froze the main thread 2.1–3.9 s (measured); now 0.4–1.3 s behind the fade with ZERO freeze, revisits 84–160 ms. Three-layer fix: THREE bakes the scene's *point-light count* into every program, so `SceneManager.padZonePointLights` buckets all zones to 6/12/24 lights (zones now share program sets); `js/scene/shaderWarm.js` pre-compiles the common material×bucket matrix at boot on the driver's parallel threads AND pre-uploads every GLB's buffers+textures in one hidden scissored render behind the boot overlay; the switch itself warm-renders only the ARRIVAL view synchronously and kicks `compileAsync` for the rest in the background. Verified with the Playwright rig (the Browser pane doesn't composite when hidden — screenshots + animation checks ran headless).
- **Labyrinth asset kit (2026-07-29, owner-requested):** an ancient-stone-maze kit generated end-to-end through the headless Rodin pipeline — 15 jobs (10 + 5 re-rolls) → **9 shipped GLBs** in `models/` (581–940 KB each): `Lab_WallStraight` (4.5×2.9 carved wall), `Lab_WallCorner`, `Lab_ArchGate` (hex stone door frame, dark recessed panel), `Lab_Minotaur` (3.2-tall axe-bearing statue on a hex plinth — the re-roll that worked used the Duskdart face rule: "standing fully upright on two legs, symmetrical, calm simple face"), `Lab_RuneStele` (carved tablet), `Lab_Brazier`, `Lab_BrokenColumn`, `Lab_Rubble`, `Lab_Shrine` (arch niche + glowing orb, best of batch). Source: `Assets/3D/Labyrinth/` — `build_labyrinth.py` (bootstrap; forced-rotation table from render sweeps — the six-axis slab test misread 5 of 10 raws), `Labyrinth.blend` (watcher-convention collections, owner-editable), raws kept beside it. **Dropped:** `Lab_Sentinel` (robed/kneeling sword-guardian statue) after 3 text-to-3D strikes — humanoid-statue-holding-object is a Rodin text-mode weakness; retry image-to-3D from a Leonardo concept if a second statue is wanted. A broken-ring gate raw (`rodin_Lab_RingRuin_raw.glb`) is kept unprocessed as a possible bonus ruin. **Wired same-day** — see the batch round above: the kit (grown to 19 GLBs by a second batch) now builds THE LABYRINTH zone on a 5-unit cell grid.

- **Vertical walk-through portals + play-feel round (2026-07-28, owner notes):** four owner complaints, all shipped. (1) **"Instead of horizontal portals, I want vertical portals the player passes through"** — the Ancient World Gate is rebuilt from scratch (`Assets/3D/Portal/build_portal.py` → `models/Portal.glb`, 111 KB): an upright half-buried stone ring (~4.2 wide, apex ~3.0, aperture ~2.6) with a vertical energy membrane, gold glyph stations, a floating spinning rune ring, and a glowing crown keystone. The membrane is an animated swirl `ShaderMaterial` applied at attach (`createPortalEnergyMaterial`), tinted teal/orange by the existing access refresh; the walk-through is a *committed* z-plane crossing (`js/scene/portalPass.js`, 8 tests) so strolling along the Breach's five-gate gallery never false-fires and teleports disarm. Locked gates seal their aperture with a toggleable `lockCircle` (r −1 when open — the collision cache shares the object, so in-place mutation is enough). Indoor gates shrink (`_addPortal(..., scale)`: ship/workspace at 0.8). Two old-era decorations died with the flat pad: `_addReturnBeacon` (its cyan spike impaled the new ring in nine zones) and the Breach's procedural rough-hewn arches (their pillars + collision sat exactly on the new walk axis). Two shading lessons: plain-color GLB materials arrive as MeshStandard and render ~3× darker than toon under the same lights (1/π diffuse) — gate stone is re-shaded to toon at attach; and the membrane must never get an outline hull (thin-shell tent lesson). (2) **"Transitioning to different zones is sticky/shuttering"** — `#zone-fade` cover driven per-frame in the game loop: fade out 0.13 s, run `switchZone` + a zoomed-out warm-up render (compiles every shader/texture in the zone once) on a fully-covered frame, fade back 0.24 s. Measured across a live walk-through switch: 679 frames, exactly one long frame (81 ms) at cover opacity 1.0, zero visible stutters. (3) **"I defeated the opponent but it took a long time for the system to understand"** — the old combat end was 1200 ms of silent overlay; now a VICTORY/ESCAPED/RESCUE banner pops the same frame the fight resolves (`#combat-result`), and victory frees the player at 650 ms (fled 450, defeat keeps 1200 for the rescue beat). (4) **No gate on Verdant Maw / Frozen Tundra** — `ENV_UNLOCK` 0 for both (portals render teal, aperture open, chips lose their padlocks; both verified enterable by walking through from the Breach), and their step-unlock entries left the pedometer shop. 332/332; verified live end-to-end: tundra→mine, Breach→Verdant Maw, hollow→tundra→mine chained crossings, locked gates orange + sealed, spaceship 0.8 gate seated in the hatch recess.

- **Frozen Tundra glacier — playtest fixes (2026-07-27, owner notes):** the owner played the glacier round and called it "a half-baked improvement", with six defects. All six fixed. (1) **"I can travel on both sides of the wall"** — and it was worse than reported: `resolveHeight` ALWAYS offers ground (y 0) and the ground plane spans the whole zone, so a shelf blocked nothing at ground level. Walking due north from the portal went straight through the riser and on under the entire glacier — which was also most of "the player is hidden with no cue where the upper level is" (that was the x-ray ghost, correctly showing a player standing in the basement). Fixed with `GLACIER_COLLIDERS`: y-banded circle chains along the shelf-1 riser line following each tile's own jittered z, straight across every ramp mouth (where the WALL must be open but the ground must not — a climber is above LEVEL_BAND 1.6 long before the riser line, so a y-0 chain is invisible to them), and around both rift floors. Five new tests sweep the full zone width at 0.15 and assert no ground walker crosses except through the west slot. Two bugs surfaced while building it: a run shorter than WALL_W still got a full 8-unit tile, which put wall AND chain 2.4 units into the west rift mouth and sealed the walk-in slot; and end tiles overhang their run by (WALL_W − step)/2 whenever tiles overlap, which buried three ramp mouths in ice (ramp gaps widened 0.5 → 1.8). (2) **Invisible barriers** — shelves 1 and 2 stopped at ±32 inside ±34 bounds, so their flanks were walls with nothing drawn; they now run to the declared bounds and let WORLD_EDGE_MARGIN do the job. The plaza stays ±26 for composition and got real walls on its three open edges (`PLAZA_EDGES`). (3) **"I can see the primitive triangles, such as the rocks"** — talus and rubble were `create_icosphere(subdivisions=1)`, 80 flat tris; now noise-displaced at subdiv 2 (subdiv 3 was tried and quadrupled the arch to 875 KB for rocks ~20 px on screen). The worst offender was actually the DUNES, big flat-shaded lofted surfaces showing every quad — smooth-shaded now. Three more triangle artifacts fell out of that hunt, all the same family: the dune's lee apron sat 0.012 above a body that converges with it at the rim, so it z-fought as a crosshatch (dropped); an inflated hull over a soft low-curvature dune pokes back through and hatches it (dunes ship hull-free with `noOutline`); and hulls over the THIN cap/lip beams hatched the snow band (hulls now cover the thick slab only). (4) **"Visual effects by the portal flicker when I move"** — the same defect as the Emberglade's ember web: the three ramp-approach PathRibbons all converge on the portal apron at (0,−14) and PathRibbons are OPAQUE, so coplanar overlaps had no stable depth winner. Every tundra ribbon now sits on its own rung of a deterministic y stack (verified in-browser: 8 ribbons, 8 distinct heights). (5) **Camera delays** — `CAMERA_LERP` 0.08 is pleasant weight for XZ, which is continuous, but height is not: resolveHeight snaps between levels and a 0.25-grade ramp at endgame speed gains ~4.2 units/s, so the camera trailed for the whole climb. New `CAMERA_LERP_Y` 0.28 applied to Y only; measured peak lag during a live climb fell to 0.35 units, settling to 0. 324/324; verified live — ground walker stops at the wall (z −20.5, was walking to −27.5 and beyond), west slot still open, ascent still 0 → 3.00 → 5.50 → 8.00.
- **Ramp-tree crowns, ember-web flicker, and the Landing Site's two doors (2026-07-27, owner screenshots + notes):** four fixes, two deferred. (1) **The crowns** — the owner spotted that the Hometree (spiral ramp), the Great Tree across from it and the Root Spires "use triangles for foliage, unlike the other rodin models". The numbers agreed brutally: `Hometree_Crown` was **560 tris over 13.6 units** (~4 per square unit) standing next to a 14,999-tri Rodin spire trunk; GreatTree 880, RootSpire_Tufts 320. `Assets/3D/VerdantMaw/regrow_crowns.py` replaces each with clustered `Jungle_CanopyMass` instances (decimated to ~3k — the sculpt the zone already instances ~40×): 560→26,681, 880→14,800, 320→11,834. Two guards make it safe, and **both were caught by measuring rather than by eye**: instance centres are sampled from the OLD crown's vertices so pruned sectors stay pruned, and a **per-sector envelope clamp** pulls any vertex back onto the old outline — without it the first pass re-buried the Hometree entrance (pruned sector went 2.5–3.2 out to 6.4–8.5, the exact regression already fixed twice), and spire tufts sagged a metre through the walkable deck. Verified after: every walkable mesh (Ramp/Junction/Trunk, spire Ledge and crown deck) **byte-identical**, footprints within 0.2–1.0 of the originals, entrance sector back to 2.6 against a ramp foot at 4.15, tufts floored at 6.82. `Canopy.blend`'s collections were renamed `Pandora_Hometree`/`Pandora_SpiritTree` and given `export_offset` so it finally rides the owner-edit watcher path — it was bootstrap-export-only, so a hand edit previously had no way out. (2) **"The orange spider web tree flickers when I move"** — the Emberglade's ember root web is 8 PathRibbon spokes crossing 3 rings, every one at the default `y 0.017`. PathRibbons are opaque, so ~24 coplanar intersections had no stable depth winner and strobed with camera motion. Fixed with a deterministic depth stack (trail 0.016 → spokes 0.020+i·0.0005 → rings 0.026+ri·0.0015), which is also the correct reading order: light lying across roots. (3) **Mine entrance** — an Ancient World Gate standing on the grass in front of the rock read as two doors to one place; the adit is now the door itself (`_addCaveEntrance`, trigger 1.8 units in front per the interact-radius gotcha), and the tutorial route from the pad is one worn **PathRibbon** ending AT the mouth, replacing the retired flat-plane-plus-floating-stepping-stones pattern. (4) **The ship** — the owner disliked the scout ship "period" and asked for a Star-Trek-class silhouette crossed with a C-130, boarded through the rear because the cargo bay is down. `Assets/3D/LandingSite/build_dropship.py` → `Landing_Dropship.glb`: high shoulder wings, upswept boom, T-tail, four warp pods in the turboprop stations, wraparound flight deck, cleated ramp to the ground; deliberately no saucer, no neck, no spine-mounted nacelles. Two defects fixed against verification renders — the aft fuselage stations were lifting with the upsweep, which put the "open" cargo ring above the ramp instead of at deck level (there was no doorway to walk into), and straight glow strips on tapering pods punched out as white shards (glow moved to intake and exhaust faces). Boarding is a walk-in at the ramp foot (3.77, −4.18 — where the old gate stood; player closes to 1.95, inside the 2.5 interact radius), and the Spaceship exit is now a doorway whose `spawnOverride` returns to that same ramp foot, so stepping off the ship and stepping back onto it are one place. Hull collision is a chain of circles that stops short of the ramp so the boarding corridor stays open. 319/319. **Deferred, and why:** the Spaceship interior has no aft cargo bay, so boarding lands in the existing station room (paused at the owner's request). (5) **The mountain, twice** (owner: "advance on the mountain"; then, on the first result: "the frame is not normal to the mountain. Also, that art is pretty shoddy") — the cone-loft draft shipped three real defects that one owner note unraveled: its cutter rotation (`-ADIT_DIR - π/2`) pointed the tunnel OUTWARD (so the "4.2-deep adit" was a 1.6 notch with the ember glow entombed in solid rock), the mouth face belonged to an off-axis mass whose local normal is ~65° against the 45° frame — exactly "not normal" — and smooth 15-vert lofts stood beside a Rodin sculpt. `Landing_Mountain.glb` is now built the way the good props were: masses → voxel remesh into ONE watertight landform → three displacement octaves (legacy VORONOI defaults to F1 soft bumps — crackle plates need `weight_1=-1, weight_2=1` for F2−F1) → the portal face FLATTENED normal to the approach bearing, with a bastion mass centred ON the bearing so the pocket keeps rock walls (the off-axis draft cut through the flank into open air) → a snug arched pocket, because `Landing_MineAdit.glb` is a FACADE with its own dark panel: the mountain owes it a flat face and a pocket its surround beds into, not a tunnel nobody can see → 2048 Cycles bake (tone zones, snowfields, grass-blend foot, per-cell boulder patchwork, AO) → same-topology ink hull at 0.13 world units (ink weight is a SCREEN-SPACE aesthetic — tree shells run ~0.05; the 0.30 smooth envelope tried first was a fat rind with dropouts — and the flip must come LAST: the EXACT boolean re-normalizes orientation and silently undid an early flip, which shipped one draft as a black-shell mountain). Two debugging lessons that each ate a render cycle: smart-project at 66° exploded the UV atlas into confetti so the whole 2048 bake read as blur (89° gives big islands), and the pocket depth-darkening keyed on distance to the infinite bearing LINE darkened a tube through the whole landform whose wall surfaced mid-face as a baked-in smudge that survived four rounds of mass/AO whack-a-mole (it is banded in depth now). The build script ray-probes mouth depth (2.2), wall enclosure and both meshes' signed volumes, so every one of those failures now fails the build instead of shipping. Seating comes from the build's printed numbers: `landAdit` at (−11.42,−11.42) sinks its surround 1.0 into the pocket with the timber 0.49 proud and dead-normal; collision is the ring + flanks + jamb guards (the walk sim caught a shove-wedge reaching 1.6 *inside* the flattened face beside the mouth) + a back-stop so the player stops just inside the timber line, in front of the facade's painted panel, prompt live. 319/319; sim: straight-in stops at t 9.70, both wedge attempts held outside the face plane.

Housekeeping note: parts of this round were committed mid-flight by a concurrent session (`bd5c63f` already carries the flicker fix and the regrown crown GLBs), so `git status` understates the diff — the ship's first-pass export landed in HEAD before its refinement pass and was then overwritten by the finished one.

Git (**flat policy, owner-set 2026-07-31**): ONE branch — `main` — and the repo lives in this folder. Commit finished rounds directly to `main`, push BOTH remotes: `origin` → `LeatherSpoon/1Under1Over` (public, `main` only) and `backup` → `LeatherSpoon/1Under1OverToo` (**private** full mirror — retired branches archive there, and it tracks the item art originals `.gitignore` excludes, ~92 MB, which nothing else can regenerate). No worktrees, no long-lived feature branches — ask the owner before creating either. Push both when a round lands, or the mirror rots.
- **The Frozen Tundra glacier — the ice-desert pivot (2026-07-27, owner-directed):** the owner played the zone and rejected the "ice desert" read; the ask was an icy world with plenty of visual FEATURE, elevation change, and explicitly **no wrap-around ramps** (the Hometree helix is not the answer twice). Measured before: 55.7% of frame inside a single 8-level luminance band, the whole field crossed in **3.4 s** at endgame move speed, everything at y 0, `env._spinners` empty, and no `ZONE_BOUNDS` entry. Now: the southern snowfield is untouched (every prop, the lake, the Hollow mouth, the portal keep their coordinates) and north of z −21.5 the ground CLIMBS three shelves — **y 3.0 / 5.5 / 8.0** — to an arch plaza, cut by two crevasses and crowned by a great ice arch, bounds 80×80 → **68×94** (minZ −62). **Why the glacier is RAISED rather than the crevasses dug:** `resolveHeight` always considers ground y=0 and returns the highest candidate within STEP_UP, so nothing below 0 is ever reachable — the rift floors ARE the base ground plane, seen from between walls of ice (pinned by a test). Elevation is kept legible at the fixed 46° camera (where a unit of height and a unit of depth project within 5% of each other) three ways instead of by circling something: stacked risers whose edges face the camera, rifts read by occlusion, and — still to build — scene swaps. `js/scene/zones/FrozenTundra/glacier.js` is the single source of truth: shelf rects, straight frontal ramps, rift flanks, spans, the arch and the dune scatter all generate from the same constants that register the walkable surfaces, and `tests/systems/glacierSurfaces.test.js` strides every route at **0.6-unit (endgame) strides** in both directions. Two real bugs came out of that test before anything shipped: ramps landing on a shelf's FAR edge (`.n`) tunnel the whole band and leave their mouth mid-shelf where nobody can join them, and the ascent route walked straight through the west rift instead of over its span. **Assets** (`Assets/3D/FrozenTundra/build_glacierkit.py` + `build_icearch.py`, GlacierKit.blend/IceArch.blend both watched): sastrugi dunes ×2 replacing the 22 `CylinderGeometry` poker-chip drifts, shelf riser, rift wall, ice bridge, and the arch (18 wide × 12.6 tall, glow vein up the camera-facing flank). Asset gotchas worth keeping: a dune lofted over a rectangular grid leaves a flat rectangular TRAY wherever its height field reads zero (footprint must taper parametrically); a mass that reaches z 0 at its own perimeter is zero-thickness there and its outline hull speckles it black (bury the base); hand-wound box strips come out inward-facing and vanish under backface culling (`recalc_face_normals`); open strips get an arbitrary orientation from recalc, so ribbons must be double-sided; EEVEE draws reversed-face hulls regardless of the cull flag (hide them for check renders — three.js culls correctly); flat shading on a big lofted wall gives every ROW its own normal, and caps built as N abutting boxes leave a seam per join — together those made the whole glacier read as scaffolding until the faces went smooth and the caps became continuous lofted beams. **Palette:** sky now DARKER than the snow so there is a horizon at all, shadow fill hard blue-violet (snow is a mirror), low warm sun, fog pulled back 26/62 → 46/108 (it existed to hide an empty north); ground is vertex-coloured cold-basin → bright-plaza via `_addGround`'s `colorAt`. Result measured in-game: single-value dominance **55.7% → 25-30%**, value spread **31% → 41-50%**. **The aurora is back and dynamic** (`aurora.js`) — the first attempt was cut because additive sky ribbons read as painted stripes on the snow, which is a property of the camera, not of auroras: these are three shader curtains standing VERTICALLY on the north horizon behind everything reachable, occluded by the plaza and the arch, so they can never project onto the floor. Plus wind-driven spindrift on every level (`env._spinners` grew an optional `update(delta)` for motion that isn't a spin) and `buildPropMesh` grew `scaleXYZ` for terrain sections. 319/319; ascent walked live at 17 u/s: 0 → 3.00 → 5.50 → 8.00 onto the plaza. **Not yet built:** the two elevation scene swaps (ice-shelf overlook off the plaza, sub-glacial gallery off the Blue Rift floor) — their mouths are placed and tested as standable in glacier.js, but the zones behind them do not exist; the frozen lake is still the old circle decal.

- **The transitional phase (2026-07-27, owner direction — Raya/Kumandra palette):** the teal→warm change is now a GRADUAL northern phase, not a cut at the gateway. Three z-driven layers, all touching nothing south of ~−58: (1) `mawGroundHex`/`mawGroundColorAt` in canopy.js — pure gradient stops (teal moss → lush green → warm olive → golden-green, band boundary wavering with x) feeding a **vertex-colored ground plane** (`_addGround` grew a `colorAt` option, Mine-floor convention) AND every northern ribbon's `groundColor` (rivers/trail/web blend into their LOCAL ground); (2) `ZONE_AMBIENCE.verdantMaw.zGradient` — SceneManager lerps clear/fog/ambient/sun/fill/player-lamp COLORS per frame from the player's z (smoothstep −64→−102; zones without the field never touch the path); (3) content by z — rivers go teal → jade → gold-green (bodies, cores, spark palettes), waymark spores teal → pale green → gold, and two new Rodin flora (`Jungle_BambooGrove.glb` — regenerated once to shed a planter box, then its "bare ground" earth mound BURIED below z=0, the pedestal-translate trick; `Jungle_GoldTree.glb` — upright first try) join the flank/rim/bank scatters north of −68, leading the mix past the gateway. Verified live: fog #102a3a at z −20 → #2a3a1c at −110, capture walk south→glade. Headless-rig lesson: a hidden-pane boot can run with 0×0 canvas + NaN camera aspect (probes work, every capture is `data:,` or solid fog) — `window.dispatchEvent(new Event('resize'))` after sizing the pane repairs it; read `toDataURL` in the same task as a forced `renderer.render`.
- **The Emberglade + Rodin quality pass (2026-07-27, owner-directed pivot):** the sanctum beyond river 4 first shipped as a recreation of Avatar's Well of Souls; the owner cut it ("too close to source material"), flagged the procedural bmesh assets as low-quality (foliage giants, Root Spire), called the river flat, and asked for a WARM scene. Now: the ground band past river 4 (spire-4 access only — the canopy pilgrimage still gates it) walks a worn trail under a **gateway rock arch** where the night turns AMBER — the **Lantern Tree** (original design: gnarled dark tree hung with glowing amber lantern-fruit; Rodin sculpt → `Ember_LanternTree.glb`, LanternTree.blend watched) stands at (0,−127) in an **ember root web** (PathRibbon spokes + 3 rings + gold sparks + orbiting emberlings via `env._spinners` — axis is a string, not a Vector3), ringed by 5 warm-stone **Glade Arches** and 3 **Sky Isles** adrift at y 9.8–12.6 (warm glow tips; visual-only, hung so their ortho footprint never covers the sanctum). No nodes, no enemies — the glade is the reward; nav chip "The Emberglade". Zone bounds 80×144 → **80×188** (minZ −148); `scatterGrottoRim()` closes the world (ring parted at the gateway, world-end wall, flank fill). **Rodin pipeline industrialized** (`Assets/3D/VerdantMaw/rodin_process.py` + build_lanterntree/canopymass/spire): socket `create_rodin_job` → poll the *download* endpoint by task_uuid (the status endpoint needs a subscription_key that's easy to lose) → headless import→join→single-512px-diffuse→orient-by-slab-test→normalize→decimate; raw sculpts kept as `rodin_*_raw.glb` build inputs. The old procedural foliage clouds are replaced wholesale by a Rodin **Jungle Canopy Mass** (`Jungle_CanopyMass.glb`, normalized to the old native height so every placement scale held; 9k tris — it instances ~40×), the **Root Spire v2** grafts the authored helical ledge onto a Rodin twisted-root trunk with the walk band widened 0.85→1.05 and pushed to rMid 2.35 (owner: descending fought the rim — same fix as the Hometree, width; the old walkable edge also poked 0.05 past the visual lip, now tucked inside), and the **rivers are two-tone** (pathStrip `coreColor` — 7-column ribbon with a luminous meandering core; opt-in, classic 4-column output untouched) with `scatterRiverBanks()` boulder/fern/shroom banks and midstream stones. Rodin gotchas logged: bbox_condition must be integers; grounded prompts bake a square grass slab (cut by z+radius face filter → root mound); "short trunk base" once returned foliage on a giant stone plinth (regenerate with explicit no-base wording). Tree-of-Souls assets deleted (GLB/blend/script); the tree's lantern-fruit faces self-glow via a texel-classified `LanternGlow` emissive split (reveal re-shade skips /glow/-named materials). **306/306**; live: new keys loaded + old foliage key gone, LanternGlow present in-scene, tree/arches/isles at authored positions, captures (glade, tree, river v2, spire v2).
- **The River Expanse (2026-07-27, owner-requested):** the corridor continues north from the North Reach apex over **four uncrossable rivers** — "river-crossing-corridor, three more times." The Verdant Maw is the first zone to use `ZONE_BOUNDS` for real: 80×80 → 80×144 (minZ −104). Each band: a wavy **glow-river** (one opaque PathRibbon each, 0x2e8fae — the first color shipped invisible against the night ground and was brightened; bioluminescent spark dots drift the water) whose ground barrier is a collision-circle chain along the exact centerline (r 2.6, step 2.4 — never a player-sized gap, pinned by test; circles carry no y so the canopy bridges cross 7 units above untouched, and a live probe confirmed the water expels a walking player 2.7 off the centerline); a **crossing pad + second pad** continuing the chain; and a **Root Spire** (`build_spire.py` → `Pandora_RootSpire.glb`, RootSpire.blend watched — mini-Hometree: 1.25-turn helical ledge, glow-lip spiral, broad mossy crown platform, walkable numbers mirrored in canopy.js `SPIRE`; placements must keep rotY 0/scale 1) so every enclosed far bank keeps its own ground access. The chain ends on **Riversend Crown** (big blossom, quartz + resin rich nodes, bloomfang guard, its own nav chip, grounded giant-foliage wall behind it so the expanse ends on jungle rather than the ground plane's edge). Band floors and flanks are filled by `scatterMawExpanse()` in ZoneAssets (seeded jungle walls |x| 19..31 + sparse undergrowth, river/stem/spire-foot keep-outs). Node/enemy content: fiber/timber/carbon_biomass one per band + a duskdart per crossing pad. Three new test guardians: the expanse seam-walks apex→Riversend, all four spires climb foot→crown, and the river chains assert gap-free coverage past the playable width; the endgame stride test now covers all 24 bridges. **305/305**; live: bounds live, 12/12 new bodies exact, spire ledge alignment ≤ 0.014, captures (crossing, band pocket between two rivers, Riversend).
- **Canopy entrance on the flow line + the North Reach (2026-07-27, owner-requested):** two asks from a play session (owner confirmed the shudder fix landed — "you fixed the flicker!"). (1) **Entrance flow** — the ramp foot sat on the trunk's south-east while every natural approach comes out of the zone's heart from the south-west; the owner's arrow said the entrance belongs on that line. HELIX `th0` rotated π/2 → 3π/4 (foot (13,−14.85) → (10.07,−16.07), 1.125 turns, grade 0.245, `th1`/junction untouched), the Root Gate moved to (8.9,−14.9) rotY −π/4 so its passage aims NE at the foot (leg collision circles now computed from rotY), trail/sentinels/threshold mat/entrance light/nav landmark all follow, and build_canopy.py's apron + gatepost + crown-prune ensemble is now **parametric off TH0** (`FOOT_DX/DY`) so the whole doorway moves with any future rotation — Hometree GLB regenerated headless, and an in-game raycast probe confirmed visible-ramp vs walkable-helix deltas ≤ 0.005 at six points. The jungle-wall giant that had been moved to (6.5,−11) last round was sitting on the NEW approach — moved again to (−2,−13); doorstep-clearing is apparently a per-reroute chore. (2) **The North Reach** — the corridor's second arc, into the empty north the owner faced: junction → **Kapok Rise** (4,−28.5, y 7.4, duskdart) → **North Reach apex** (−4.5,−32, blossom, rich resin node) → **Fern Shelf** (−13.5,−28) → Sky Altar, four computed bridges added as LOOP_LINKS entries (stride/seam tests and waymark spores extend automatically; the loop seam test now walks main loop + arc continuously and ends on the Altar). Mid-arc stands the **Great Tree** (`build_freetree.py` → `Pandora_GreatTree.glb`, GreatTree.blend watched): a leaning glow-veined giant at (−3.8,−27) that is deliberately **unconnected to the canopy level** — no pad, no bridge, no surface; every span clears its crown by ≥ 1.4 and it collides only at the trunk (owner: "a tree that does not attach to the second level"). Backdrop foliage giants that stood inside the new arc moved behind it (z −34..−36); new under-pad crown + three under-bridge fills keep the anchoring rule. 302/302; live sweep (3 pads, tree, gate all exact) + captures.
- **Canopy: shudder fix, anchored masses, findable ascent (2026-07-26, owner screenshot with red circles):** three notes, three fixes. (1) **"The illusion folds / breaks into a shudder"** — every red circle was a variant-B blossom pad: the v2 soft-flatten crushed *everything* in the walk cylinder to deck height, folding petals flat (sheared faces = the white streak needles) and stacking layers into exactly-coplanar sheets (z-fight strobe). build_pads.py v3 re-imports the pristine Rodin sculpts (task-uuid ledger) and flattens only a vertical band around deck: tall petals keep their shape (the blossom ring is back — the reveal cut handles occlusion), petal skirts feather instead of shearing, buried coincident layers tuck 0.15/layer below the floor (B had **566** such verts — the shudder, quantified; A had 20, which is why A never strobed), fully-flattened verts get ±1.2 cm seeded jitter so no independent sheets stay coplanar, and stale baked split normals are cleared. (2) **"Branches hovering with no purpose"** — bridge visuals now extend 0.7 past each ramp end along the same 3D line (tips bury into the irregular pad rims; walk ramps unchanged), and all 16 foliage clouds are anchored to structure: under-pad crowns wrap the blossom stems (pads read as treetops), under-bridge fill rides the long-span midpoints, undergrowth mounds sit on the ground, and the northern backdrop is grounded *giant* clouds (scale ~2, tops at pad height) instead of floating lumps. (3) **"Arriving is not easy / neither is understanding the path"** — a procedural **Root Gate** (build_gate.py → `Canopy_RootGate.glb`, CanopyGate.blend watched; gnarled self-glowing horns, beacon bulbs, shelf fungi) straddles the trail at the helix foot with a glowing threshold mat (the doorstep grammar) and two flanking helicoradian sentinels; **42 waymark spores** dot the approach trail, the helix outer rim, and every bridge mouth; and a new `env._addNavLandmark()` → main.js navTargets hook gives the ascent a **"Canopy Ascent" nav chip** like portals get (verified live: chip + 32m from the portal). Also removed the vine curtain that hung squarely between camera and gate — the exact entrance-burying stack from last round. Two bugs caught in verification: a TDZ crash (GROUND_CIRCLES read ROOT_GATE before its const — killed boot silently; diagnosed by re-importing main.js to surface the cached module error) and the first gate build being invisible at night (fixed with self-emission — night-zone rule: living landmarks glow). 302/302; live world-position sweep (gate, 16/16 clouds, 8/8 pads, 42 spores) + captures.
- **Canopy grand loop + descent fix (2026-07-26, owner-requested):** the owner hit two things playing the climb: going back DOWN the helix fought "collision", and they wanted "many, many more corridors of canopy." (1) **Descent** — the real culprit was the axis-aligned edge-slide: on a *curved* rim both single-axis fallbacks routinely fail at once, so walking against the band edge halted dead. main.js now **edge-follows** — the blocked step retries at ±0.5/±1.0/±1.35 rad rotations and takes the first legal direction — so pushing into any curved rim glides along it; helix bands also widened (walkable halfW 0.95→1.08, still inside the visual ledge). Measured live: holding one key into the down-curve now travels 3.5 units/1.6 s around the band instead of jamming. (2) **The loop** — canopy.js is now data-driven (pads + links; bridges are *computed* rim-to-rim, so a new pad + one link line grows the network): six new pads — West Bough (7.4), Hamlet Overlook (6.8), Mid-Jungle Bough (6.2), Idol Watch (6.9), East Rise (7.6), Banyan Step (7.0) — circle the whole zone from the Sky Altar back to the junction, 19 walkable surfaces total. New `Pandora_BranchBridgeLong.glb` (native 8, deeper sag, hanging glow-tip tendrils) serves spans > 5. Content up top: 3 more rich nodes (timber/carbon_biomass/silica), a second Duskdart, a Vineclaw on East Rise, glow shrooms placed at pad height via ZoneAssets `y`. Tests walk the full loop at 5 cm steps + pin every bridge grade < 0.45 and every endpoint inside its pad; verified live end-to-end (Altar→junction, y 6.2–8.2, no ground touch, zero console errors). **Owner asset-philosophy clarified mid-round:** Blender is the hub and the Rodin bridge is one of its features ("if it can be used in Blender — USE IT"), owner granted standing permission to install Blender add-ons, and **every asset now lands as an openable .blend source** — Canopy.blend, PandoraFlora.blend, Duskdart.blend (VerdantMaw), Knoll.blend (LandingSite); the build scripts save them alongside every export. 302/302.
- **Canopy corridor quality round (2026-07-26, owner pushed back twice — "low quality", "I want higher quality than that!"):** the graft-a-disc pad construction could never clear the bar (perfect circle + UI glow ring + flat top), so it was replaced wholesale. Each pad is now ONE Rodin sculpture — variant A a gnarled mossy table with drooping moss-icicle edges and a rooted trunk, variant B a blue blossom with veined petals and a twisted vine stem — with the walk zone **soft-flattened in Blender** (dead level r<3.4, feathering to fully organic rim lips past 4.3; walkable math untouched) and no geometric ring at all. The corridor also gained ENCLOSURE: 15 Rodin foliage clouds (low on the camera side so they never hide a walkway, tall fill north) + 7 hanging vine curtains under the long spans + the drifting glow motes. Sources: PandoraPads.blend + PandoraDressing.blend (both watched); pad builder retired from the bootstrap runner; `_applyRevealShading` keeps diffuse maps. **Two self-inflicted bugs caught and fixed in verification:** an O(N²) deck-scan (max() inside a per-vertex filter) froze Blender for ~12 min — hoisted, now instant, and the stride/seam suites plus a live world-position sweep (every pad/curtain/cloud body vs its authored placement, all exact) closed the loop; and the export-offset dance subtracted the source-parking offset *before* adding it, baking −12 into pad-B/curtain GLB nodes (pads rendered west of their pads, curtains upside-down as "petal bursts") — export now happens at origin, park after. 302/302.
- **Canopy entrance unburied + Rodin branch bridges (2026-07-26, owner notes):** two art fixes. (1) **The climb's entrance was obscured** — three stacked causes, all fixed: the Hometree's crown south sector hung directly over the ramp approach (pruned open — the entrance sky is clear from ground to the first quarter-turn, crown mass rebalanced north), a leftover jungle-wall giant at (13,-12) parked its foliage on the doorstep (moved to (6.5,-11)), and nothing led there (a worn PathRibbon trail now runs portal → east of the idol → the ramp foot, ending on a new root apron flanked by two luminous waymarker spires, entrance light 3.0→4.5). (2) **The scripted branch bridges read low-quality** — replaced with Rodin-generated living branches (short 4u + long 8u): grained bark, glowing shelf-fungi and moss, hanging tendrils; processed to length/width/deck-line in Blender, source `Assets/3D/VerdantMaw/PandoraBridges.blend` (watched), scripted bridge builders removed from the bootstrap runner and their collections dropped from Canopy.blend so nothing can clobber the Rodin GLBs. `_applyRevealShading` now carries the diffuse map into the reveal material — textured props no longer collapse to flat color under the canopy reveal cut. Same GLB filenames, so placements/aim math unchanged; walkable geometry untouched. 302/302; verified in-game (mouth capture, bridge close-ups).
- **Bridge-mouth traversal fixed for real (2026-07-26, owner-reported "still intraversable"):** the edge-follow round fixed curved-rim sliding but the owner (playing at endgame ~17 u/s) still hard-stuck at the Junction↔Bough↔Altar bridge mouths. Root cause was geometric, and speed-dependent — which is why base-speed verification missed it twice: those two hand-authored bridges anchored their endpoints deep inside the pads, and the resolver's highest-rule holds the player at pad height across the overlap, so the rim crossing demands a drop of Δy·(overlap+stride)/len in ONE frame — over STEP_UP at speed. Fixes: BRIDGE_A/B now computed rim-anchored like the loop (`bridgeBetween`), the Sky Altar retuned 8.2→7.8 and shifted west (1.6 of height over a 2.2 gap survives no bridge shape — 1.2 over 2.9 does), STEP_UP 0.55→0.7, and a **stride test** now walks every bridge in 0.6-unit endgame strides both directions (it caught the residual cliff my first fix left). Verified with real key input at moveSpeed 17: single key-holds sail Junction↔Bough↔Altar in both directions, the loop crossing + long bridge chain flows, and diagonal shoving into bridgeless rims holds safely. 302/302.
- **Owner-editable asset pipeline (2026-07-26):** `watch-assets.bat` (repo root) watches the .blend sources and auto-runs `Assets/3D/export_blend.py` (headless Blender) on save, re-exporting each marked collection to `models/` — so the owner can edit assets in Blender **without a Claude session**: edit → save → reload the game tab. Convention: one top-level collection per GLB, named exactly like the output (e.g. `Pandora_Hometree`), carrying an `export_offset` custom prop; collections with an armature export as NLA_TRACKS. Verified end-to-end: all four .blends round-trip to byte-equivalent GLBs, and a live save auto-exported in ~10 s. **The build_*.py generator scripts are bootstrap-only — re-running one overwrites hand edits; once a .blend is hand-edited, the .blend is the source of truth** (warning also in export_blend.py's header).

- **Pandora round two (2026-07-26, owner-approved follow-up):** three hooks from the canopy round, delivered. (1) **Rodin flora** — `Pandora_Helicoradian.glb` (coiled spiral plant, upright first try) and `Pandora_PuffballTree.glb` (glow seed-puff under a dark arch; imported lying, stood +90°X), 5 placements in the Maw. (2) **Duskdart** — canopy-native tree gecko on the Gathering Bough (y 6.6, patrolR 0.8): big glossy eyes, violet glow spots, spiral tail; quad-rigged per the tundra recipe (14 bones, Idle/Walk NLA), full wiring (archetype/drops/codex/spawn). This was a **re-roll**: the first gen ("Glimmerfan", rainbow head-fan) was rejected by the owner — fan on the head + muddy face; the fix that worked was designing around large simple eyes ("two very large glossy dark eyes, clean simple friendly face, no crest, no fins, no fan") and rendering a **face close-up before rigging**. (3) **Lookout knoll** — the Landing Site's northern meadow got a climbable outcrop (`zones/LandingSite/knoll.js` + `build_knoll.py`, same one-source-of-truth discipline): 3/4-turn shelf ramp to a grass ledge with a rich copper lode; proves vertical space is now ordinary zone work. Gotcha re-learned the hard way: the runtime auto-hull painted the knoll's single-quad step **solid black** (the tent lesson, on my own authored GLB) — fixed by baking a `Knoll_OutlineHull` for the closed masses so the game skips its runtime hull entirely. Also: the shared Blender instance's scene was wiped mid-pipeline by an outside File→New (again) — recovery = re-import by saved task_uuid, then run the whole normalize→rig→export pipeline in a single socket batch. 300/300 tests; everything verified live (Duskdart modeled + animating on the pad, knoll summit resolves 3.0 with the node reachable, captures taken).

- **Verdant Maw = Pandora, and the engine learned UP (2026-07-26, owner-requested):** the deferred "real vertical levels in one zone" work is SHIPPED. Engine: `js/scene/walkableSurfaces.js` — pure disc/rect/ramp/helix surfaces + `resolveHeight` (highest-candidate-within-STEP_UP rule; nearest-to-current would pin a ground player at 0 forever, pinned by test), player edges block with axis-slide (no falling physics), collision circles/node circles carry optional `y` with a 1.6 level band, enemy/node spawns take `y` (+ `patrolR` leash — enemies never height-resolve), camera rig + player lamp follow player y, ZoneAssets grew `y`/`reveal`/`aim` placement fields. Zone: the **Hometree** (NE) carries a 1.25-turn helical ramp to a canopy layer — junction ledge → branch bridge → **Gathering Bough** (rich resin+fiber nodes, y 6.6) → bridge → **Sky Altar** (y 8.2, Spirit-Tree glow willow + bloomfang guard on a 0.8 leash). Whole ascent lives ONCE in `zones/VerdantMaw/canopy.js`; the builder, the 5-cm-step seam test (`tests/systems/walkableSurfaces.test.js`) and the Blender script (`Assets/3D/VerdantMaw/build_canopy.py`, 4 GLBs authored via MCP port 4444, 46–215 KB) all mirror it — in-game raycast probes measured visible-vs-walkable deltas ≤ 0.007 on the ramp, 0.00 on pads (the junction ledge was walkable-but-INVISIBLE until a probe caught it; "every surface needs a visual at its exact height" is now a CLAUDE.md rule). Re-theme: `verdantMaw` ambience is bioluminescent night (teal-indigo, pale moon, violet fill, teal bio-lamp), dark moss ground, 11 more glow shrooms, warm hamlet doorstep lanterns, canopy accent lights; reveal-shaded pads/trunk open around a player beneath; codex copy touched. Verified live end to end: scripted route climb 0→7.2→6.6→8.2 with camera at 22.0, rim hold under real key input, ground-under-pad stays 0, canopy nodes gated by 3D distance, guard patrols at 8.2. 298/298 tests; 5 in-game captures via the sink rig. *Next round hooks:* helicoradian/puffball Rodin flora pass, a canopy-only creature, floating-mountain skybox silhouettes.
- **Atlantis south corridor (2026-07-26, owner-requested):** the grotto wall ring is now broken at its southern point and a walled corridor runs out to a small drowned end chamber with a still pool — the city's back door. The ring loop skips a 0.52-rad mouth; corridor and chamber reuse one extracted `grottoBlock()` so the new rock matches the ring exactly; the south processional route was extended to run the whole way out. Verified by scanning the real collision circles for the widest walkable span at each z (3.1–4.6 units all the way, centred on the route; sealed at z 35) and by walking the player from the plaza to the far wall without snagging. Dead end for now — **the Meltwater Rift's Sunken Door is the natural thing to put behind it**, which would finally make that sealed gate real.
- **Player lamp · walk-into mining · Atlantis gate relocated (2026-07-26, owner-requested):** three features on top of the Mine round below. (1) **Lamp** — `SceneManager._playerLight`, a PointLight following the player from `update(playerPos)`, tuned per zone via a new `ZONE_AMBIENCE.playerLight` (caves 6.5-9 at distance 14-16; the shared daylight default is a faint 1.6/9, since a lamp that reads outdoors blows out everything you walk past). (2) **Walk-into mining** — pushing into a mineable block starts the same dig `[E]` does; `_beginRockGather()` is now shared by both entry points, so both charge identical energy (verified: 3 and 3) and run the same timer, and the block still blocks until mined out. Two guards make it usable rather than infuriating: it fires only on **face** contact, never corners, and only past a 0.8 push dot. Corners matter because the normal there is diagonal — a diagonal move reads as dead-on, and since blocks abut, squeezing along a corridor clips the next cell's corner at every boundary and would dig the whole wall you were walking past (this actually happened in testing before the guard). Verified: heading straight at a face tunnels 14 units through the mass; squeezing along a wall on W+A travels 7.5 units and digs nothing. (3) **Atlantis gate** — asked to move west; moving it west *along its own row* would not have helped, because the Breach's back row (23) sits in the shadow of the map's 6.4-unit south shell wall and is invisible from the fixed camera. That is why the gate was unfindable, and the Lagoon Coast gate on the same row shares the problem. It now stands at (-19.2, 28.8), the west end of row 21 — the row where Verdant Maw and Frozen Tundra plainly read — 9.6 out from Verdant Maw, matching their spacing. Verified clear line of sight by raycast, plus locked hint → clearance → enter → land in Atlantis. **Lagoon Coast followed (owner request):** it had the identical bug on the same row and now mirrors Atlantis at (19.2, 28.8). Row 21 is now a single **gate gallery** — Atlantis (-19.2), Verdant Maw (-9.6), the ancient ring (0), Frozen Tundra (9.6), Lagoon Coast (19.2) — with both end gates' arches turned to face the ring like their neighbours; all four project on-screen at one depth, evenly spread. The Breach anchor room and cross-gallery grew a column at each end. Row 23 is now a decorative south alcove and should never hold a gate again. 289/289; all six gates on floor with orthogonal access across the baseline map and 200 seeds.

- **Mine: the rock mass is solid now (2026-07-26, owner-reported):** the mine only ever instantiated *exposed* mineable cells, so the cave was a one-cell-thick shell of rock standing on open floor — **250 of 336 mineable cells (74%) drew nothing at all**. Two visible symptoms, both reported by the owner: you saw straight past a rock face to the lit floor behind it (floor is pre-built under every mineable cell), and digging appeared to *create* the next rock instead of revealing it. Fixed with `getMineFillCells()` (layout.js) + per-chunk `fillCells`/`fillObjects` (Mine/index.js): every buried cell gets a visual-only piece — no logic object, no collision, no loot — through the same `_materializeWallCell` the solid walls use, and `_promoteFill` drops it when a dig exposes the cell. Because wall and plain-rock materialization consume the same `cellRng` roll sequence, the filler and the block it becomes are the same piece at the same rotation and scale: verified in-game that a dug neighbour's cells went `filler wall_worked_b rotY 1.5708 scaleY 1.0985` → `rock wall_worked_b rotY 1.5708 scaleY 1.0985`, i.e. **no pop-in at all**. Cost +138 draw calls / 3.8→6.0 ms render, bounded by the existing chunk window. Measured and rejected: culling fill deeper than 5 cells from open floor is pixel-identical across 54 vantage points but saves only 32 of 247 meshes (a first eyeball read said depth 2 looked fine — a pixel diff proved it wrong at 42 of 54 positions, up to 24% of the screen). 288/288; stress-tested by digging 120 blocks with zero double-renders and zero holes. **Next for the Mine:** the mass now reads as a repeating grid — only 2-3 wall variants per region with ±10% height jitter, so the crest line is nearly flat across a whole face. That's Blender kit work, not JS.

- **Nav aid — long-range fix + locked-gate state (2026-07-26):** the aid below shipped with a bug that made it blind at exactly the range it exists for, found when the owner couldn't locate the Mine's Atlantis gate. `computeOffscreenIndicators` rejected targets whose projected `z` fell outside ±1; but the camera sits only 14 up / 13.5 back, so **anything more than ~25 units ahead of the player in +z projects behind the near plane** while still having a perfectly good bearing (the five Breach gates, 39-65 units up the shaft, all landed at z ≈ -1.05..-1.25 and never drew a chip). The clip test is gone — the camera is orthographic, so there is no w divide and NDC x/y are valid at any depth. Two follow-on fixes the working aid then exposed: chips pinned to the same edge overlapped into mush (all five Breach gates inside ~200px of bottom edge), and a long label on the right edge hung off-screen, because NavAid works in NDC and can't know a chip's pixel width. Both now handled in one measured-box pass, `HUD._layoutNavChips()`: clamp each chip inside the viewport, then greedy row-pack — the along-edge axis carries the bearing so it stays exact, overlapping chips step one row inward, nearest keeps the outer row (widths cached on the element and re-measured only when the text changes, so the per-frame call doesn't force a layout). Locked gates now also *read* as locked: `refreshPortalAccess()` stores its verdict on the portal record (`portal.accessible`) instead of only tinting the gate material, so a chip can show `🔒` + a dimmed style — amber stays reserved for bosses, so locked reads as inert, not as a warning. Walking to the gate still gives the full unlock condition via the existing proximity hint. The screen math also moved out of the three.js file into a pure `js/ui/navAidLayout.js` (same split as `PathRibbon.js`/`pathStrip.js`) so it is finally testable in Node — `tests/ui/navAidLayout.test.js` pins the depth-independence regression by name along with edge pinning, in-viewport guarantees, bearing preservation under clamping, and the nearest-first cap. Verified live in 9 zones + 3 positions along the Mine shaft: 0 overlaps, 0 off-screen boxes, correct show/hide as landmarks cross the view edge, and the padlock clearing the instant Unmaker clearance lands. 284/284.
- **Off-screen landmark nav aid (2026-07-26):** small HUD feature delivering decision D2 of the Expanded Biome Worlds plan (below) — edge-of-screen arrow chips point toward portals and live bosses in the current zone once they scroll off the camera's ~14-21 unit reach, each showing a label + distance. Pure projection math in `js/ui/NavAid.js` (`computeOffscreenIndicators`, uses `THREE.Vector3.project(camera)` against the live camera so it self-corrects if zoom/frustum ever change — no hand-rolled trig), rendered by a pooled-DOM `HUD.updateNavAid()` called unthrottled every frame from `main.js`'s game loop (separate from `HUD.update()`'s 100ms throttle so arrows stay smooth). Targets today are portals (`env.getPortals()`, sealed future-gates excluded) and live bosses (`entityManager.enemies` filtered on `.boss`); capped at 6, nearest-first. Built zone-agnostic on purpose — works on all 15 existing zones today, not just future biomes (verified live in both Landing Site and the Mine's 5-gate Breach chamber via the headless rig: correct show/hide as the player crosses each landmark's visibility boundary, correct distances, correct 6-cap sort — **but only at close range, which is how the long-range blindness above got through: both check positions stood within ~20 units of their landmarks. Verify a HUD aid at the far end of its intended range, not just where it's convenient to stand.**). 278/278 tests (no test coverage added for this feature yet — it's DOM/camera-projection UI, exercised via the headless rig instead).
- **Expanded Biome Worlds — D1-D5 resolved (2026-07-26):** the five owner decisions blocking Phase 2 (see below) are answered and recorded in `Plans/Expanded-Biome-Worlds-Implementation.md` §2 — biome footprint grows past 100×100 (exact number set during Phase 2 measurement), a `moveSpeed` soft cap is wanted but **deferred to its own dedicated session** (do not fold into world-building work), landmark legibility is solved by the nav aid above rather than by zoom or sightline composition, Depths stays on the Mine's cave stack, and the Verdant Maw dry-run checkpoint (Phase 3.5) is confirmed. **Still blocked:** Phase 1.2 (round-trip the current Lagoon Coast through the pipeline) and Phase 2 (block out the larger footprint) both need Blender / blender-mcp reachable from a session — not yet confirmed available.
- **PathRibbon — circle-patch trails retired game-wide (2026-07-25, owner call):** the overlapping translucent CircleGeometry path blobs (Atlantis processional, Meltwater Rift trails, Frozen Tundra trodden path) read as placeholder-quality and are gone. New `js/scene/PathRibbon.js` + pure `pathStrip.js` (headless-tested): each route is one merged vertex-colored toon strip — Catmull-Rom centerline, seeded width/wobble noise, edges blending to the zone's exact ground color (fully opaque — no transparency, no depthWrite, no decal-strobe risk) — in worn mode (rift stone; trodden snow with a `strength` boost for bright ground) or paved flagstone mode (Atlantis, plus stone curb rails at its four canal crossings). A backface-culling winding bug (left-handed tangent/normal frame) was caught in browser verification and is pinned by a test. 278/278; re-verified in-browser in all three zones. The Rift's ice-sheet circle patches are the same visual family but that zone was mid-rework in a parallel session — left for that effort.

- **ATLANTIS — the drowned world beyond the Breach (2026-07-25):** the 14th zone, an ATLA/Ghibli sunken city with Disney-Atlantis color logic (dark teal depths, cyan glyph-light on blue-grey stone, warm sand + coral accents). The Breach grew a **fourth world gate** (baseline map row 23 widened one cell; the gate rides `MINE_ZONE_PORTALS` so the generator's flood-fill and dressing keep-outs picked it up with no other changes), gated by **The Unmaker's clearance** — `boss_depths.unlocks`' first use — or a 20,000-step spend (ENV_UNLOCK 50000 is only the gate marker). The zone: reveal-wall sea-grotto, Great Plaza inside a glowing ring-canal with four bridges, the Crystal Heart on a dais at its center, a brazier-lined processional way from the gate, a drowned pool where a colossal guardian head stands in the water (west), a wreck on a warm sand shallow (NE), a ruined temple-dome quarter (SW), and a glyph trilithon monument on the south path. **14 Rodin text-to-3D assets in one batch, 0 re-rolls** (`models/Atlantis_*.glb`, diffuse-only, 1.0–1.5 MB each, export-normalized to true world scale): the guardian head needed a six-view render sweep to find its face (lying, pitched, inverted — three corrective rotations); the "archway" generated as a squat 4-leg glyph trilithon and ships as a roadside monument instead of a walk-under. Gatherables reuse existing types (silica / silica_sand / quartz / silver — no new materials); fauna borrows cavecrab / spoonvark / blubberfin; **deliberately no resident boss this round** — the Unmaker's clearance is the price of entry. All translucent floor decals carry `depthWrite: false` (rift strobe lesson). Blender source: `Assets/3D/Atlantis/Atlantis.blend` (MCP instance on port 5555). 272/272 tests; verified in-browser end to end: locked hint at the gate, clearance → enter → spawn, return gate → mine spawn, codex auto-discovery. *Follow-up hooks:* the Meltwater Rift's Sunken Door is the natural second (walk-in) entrance; a resident boss + chapter rung is the obvious next round.

- **Landing Site native pack (2026-07-25):** the starting zone — the game's first impression — was the last one still dressed in generic pre-Rodin starter props, so it got its own art end to end. 14 Rodin text-to-3D generations, 0 re-rolls. **Ground cover:** grass tufts, wildflowers and shrubs seeded across the meadow by `scatterGroundCover()` in ZoneAssets (150 entries from a fixed seed, emitted as ordinary placement rows so they stream and late-attach normally); the flat-green bedsheet is gone. **Survivor camp** east of the pad — tent, campfire, log seat, crates — with the game's first NPCs outside the Maw: **Mara** (quartermaster) and **Finch** (scout), rigged breathing-spine Idle rigs facing each other across the fire. **Mine adit:** the tutorial dirt path used to end at a blank grey cone; a timber-framed stone arch is now cut into the mountain's foot with the Ancient World Gate standing in its mouth, and the mountain itself was rebuilt from one 8-sided cone into a five-mass ridge. **Creatures:** **Mossback** (plated grazer, the safe first kill), **Burrfang** (fast frail pack hunter), **Stiltbeak** (long-legged wader with a reach jab) — deliberately the gentlest roster in the game, replacing the borrowed Mine `serpendrill`; plus a dedicated `Boss_ScrapTyrant.glb` (was sharing Pirate_Lizard with 3 other bosses) on a new trampled-earth arena. **Outer ring:** content stopped at the r=14 treeline inside an 80×80 world, so the boss fought on blank grass — a thinning belt of trees now runs r≈18→33 with outcrops, fallen logs and 3 outer resource nodes. New engine convention: **`noOutline: true`** on a placement skips the runtime ink hull — required for thin open shells (the tent rendered solid black without it) and for scattered cover (inky blobs, and it halves draw calls). 272/272 tests; verified in-browser via the headless rig at every location.

- **Expanded Biome Worlds — foundation built, biomes not started (2026-07-24):** the four Mine branches (Verdant Maw, Lagoon Coast, Frozen Tundra, Depths) become separate 100×100 worlds with a traffic-hierarchy route grammar, Blender-authored layout data, and spatial sectors. Design: `Plans/Expanded-Biome-Worlds-Design.md`; build order, findings and build log: `Plans/Expanded-Biome-Worlds-Implementation.md`. **Shipped this round (the plan's F1–F4):** the ±39 global clamp is gone — world size is per-zone via `CONFIG.ZONE_BOUNDS`, so enlarging a zone is a one-line entry (existing zones deliberately unchanged; blank ground reads badly without content); the authored-layout pipeline (`layoutSchema.js` with all seven export rules enforced in `npm test`, `LayoutBuilder.js`, `modelKeys.js` pinned to Environment's `_glb`, `Assets/3D/BiomeWorlds/export_layout.py` + conventions README); and `SectorView.js`, built clean rather than extracted, so the Mine adopts it later rather than constraining it. 272/272 tests; verified in-browser across all 13 zones. Measured: generous sector radii make sectoring a no-op at 100×100 (56/72 left 96% of props live), so defaults are size 10 / 34 / 48 — worth ~2× at this map size, not an order of magnitude. **Next:** see the "D1-D5 resolved" bullet above — the five decisions are answered, Phase 1.2/2 are blocked only on Blender/blender-mcp access.

- **Meltwater Rift — junction zone under the Glacial Hollow (2026-07-24):** the 13th zone, entered by walking the Hollow's wall-ring gap the Rimefather guards (warm light now spills up through it; new crystal/rubble props funnel onto the trigger); the return gate lands back on the rift's doorstep via `spawnOverride` on a plain portal record. A two-temperature cavern — ice crust + cyan light at the entry shading to basalt + ember light deep — with a glowing meltwater channel, hot-spring terraces, hex basalt columns, steam vents, and a worn path forking to **two sealed future-zone thresholds**: the **Sunken Door** (west) and the **Ember Chasm** (east), built on the new `env._addSealedGate()` record (proximity hint, no enter action; swap for a real entrance when those zones ship — CLAUDE.md door-zone paragraph). Two new gatherables — **obsidian** (Cryo-Pick-gated) and **embermoss** — wired through inventory/node visuals/icons/codex. Fauna: reused cryolisk/chillwing, deliberately no boss; all props procedural (no new GLBs). 231/231 tests; verified in-game headless (entry prompt + E-through, both sealed hints, gather + tool gate, codex discovery, doorstep return).
- **Enterable home interiors + scroll-wheel zoom (2026-07-24):** the three hamlet homes now open — glowing door mats on each doorstep `[E]`-prompt into per-owner interior zones (`homeSylva`/`homeBram`/`homeSprig`, zones 10–12): tiny lamplit rooms (shared shell in `zones/HomeInteriors/` — wood floor disc, tall far-wall arc + low camera-side rim, hanging lamp at mine-lantern intensity) furnished with 9 Rodin text-to-3D pieces at true world scale (Sylva: leaf-blanket cot, hanging herb-and-lantern rack, teapot table; Bram: tool workbench, leaf-planter bed — a happy misread of "bedroll" — rope-and-shield stand after one re-roll for floating scythe shards; Sprig: brass-gadget bench, leaf hammock, copper-pot stack). New wiring: `env._addDoorway()` (noGate portal + door mat + **spawnOverride** — exiting a home lands on its own doorstep via a new optional `switchZone(zone, spawn)` arg), per-room `ZONE_AMBIENCE`, codex lore per home, `TOTAL_WORLDS` 9→12. **Scroll-wheel zoom**: wheel over the canvas scales the ortho frustum ×0.45–×1.5 with smooth lerp (`CONFIG.ZOOM_*`); HUD panel scrolling unaffected. 231/231 tests; verified in-game (zoom clamps, all three rooms, exits).
- **Glacial Hollow — new cave zone off the Frozen Tundra (2026-07-24):** the game's 9th zone and its first *walk-in* entrance. A `Hollow_CaveMouth.glb` sits on the tundra's north-west ridge at (-15,14); walking up prompts `[E] Enter Glacial Hollow` and switches scenes into an enclosed ice cavern (return gate south to the tundra). New wiring: `Environment._addCaveEntrance()` registers a portal with `noGate: true` so no Ancient World Gate spawns, and the mouth prop supplies collision — CLAUDE.md documents the interact-radius gotcha that pattern exists to dodge. The builder lays a reveal-material wall ring (fragments open around the player, as in the Mine), a frozen pool, procedural ice pillars and cyan point lights; a new `glacialHollow` ZONE_AMBIENCE takes the cave near-black. **Four new rigged creatures** (Rodin bodies rigged via `Assets/3D/GlacialHollow/rig_creatures.py`): **Rimeburrow** (blind mole-badger, frostbite burst), **Shardback** (crystal-plated pangolin tank, armor 16), **Cryolisk** (sprawling cave salamander, frostbite + rage ramp), **Chillwing** (upright cave bat, 30% dodge + FP drain) — three quad plans plus a biped plan for the bat, each with Idle/Walk NLA tracks. Plus 7 cave props (mouth, stalagmites, ice crystals, frost shrooms, rubble, mammoth skull, bone arch), new ore seams, drop tables and codex entries. **Boss + chapter rung (same-day follow-up, owner call):** the **Rimefather** (`Boss_Rimefather.glb`, a glacier-armoured woolly mammoth) guards the cavern's deep end past the mammoth-skull landmark, and is now story rung **S6 at chapter 11** — which pushed The Unmaker 11→13 and moved warden indexing to `rung <= 12 ? rung/2 : rung - 7`. Existing saves cannot regress (`highestEver` is monotonic); a test pins that migration plus a story-rungs-odd-and-gapless invariant. 15 Rodin jobs / 2 re-rolls; the "tall narrow frozen waterfall" failed twice (Rodin won't do tall-and-thin) and ships procedural instead. 231/231 tests; verified in-game via the headless rig — entrance prompt, both transitions, all 8 creatures skinned and animating, boss framing, codex auto-discovery.
- **Tundra enlargement + fixes (2026-07-24):** the Frozen Tundra field grew to roughly x -30..30, z -24..30, with the pine line carried down into the near ground below z 17 that used to read as blank white (plus widened flanks, 2 more ore nodes and 2 more creature posts so the new ground isn't dead space). A trodden path now runs east from the cave mouth across the southern flat. **Cave entrance fix:** the mouth's opening faces the camera (+z), so the old trigger at z 12.4 sat *behind* the rock and forced the player to walk around it — moved to z 15.9, in front of the opening. **Blubberfin fix:** the shipped `Blubberfin.glb` was inverted (flat belly up); re-exported with a 180° roll about its long axis, which keeps the head at -Y. All seven creature GLBs now pass a flat-base orientation check.
- **Maw-tender NPC hamlet (2026-07-24):** the game's first NPCs — three rigged plant-folk generated by Rodin **image-to-3D from the owner's character sheets**: **Elder Sylva** (willowy leaf-robed sage), **Grovekeeper Bram** (bark-armored guardian, scythe-staff generated in hand), **Sprig** (sprite tinkerer; its floating seed-drone ring generated with the model and kept). Each idles outside its own home in a new NW clearing of the Verdant Maw — grown pod-house, leaf-thatch log lodge, hollow-stump porthole den (text-to-3D, baked envelope outline hulls; pod re-rolled once for a real door — concrete "arched rounded wooden door" phrasing works where "teardrop doorway" didn't — then its rear terrace-slab cut positionally). New runtime pattern: `env._addNpc()` ambient NPCs (Idle-only breathing-spine rigs, mixers ticked in `env.update`, portal-style late attach); pipeline scripts in `Assets/3D/VerdantMaw/`. Sporeback spawn moved out of the clearing. 229/229 tests; verified in-game. Rigged-GLB export bind gotcha documented in CLAUDE.md.
- **Mine native pack (2026-07-24):** the three leftover unprocessed `Assets/3D` bodies rigged and shipped as the Mine's own T2 creatures — **Scalerunner** (blue scale-plated tunneller, fast evasive burst), **Duneplate** (boulder-shelled digger, windup tank, `corrosion`), **Bramblemaw** (thorn-backed brute, rage-ramping). Same recipe as the tundra/Maw packs (`Assets/3D/MineCreatures/MineCreatures.blend`): 84 Rodin part-materials collapsed to one 512px diffuse each (normal/metallic stripped), quad armature + 1/d⁴ nearest-segment skinning, parametric Idle/Walk on per-creature-named NLA tracks. Mine spawns are now depth-graded (skirmishers at the adit, tank + brute in the deep cuts); Dunkraza keeps the Depths shaft. GLBs are 0.9–1.3 MB — leaner than the earlier packs, which shipped all three PBR maps. Gotchas hit: one creature's rear-right leg had to be hand-placed (foot detection grabbed its tail tip), and Bramblemaw's source diffuse was near-black (mean linear 0.107) so it read as an unlit blob in the dark Mine until gamma-lifted to ~0.28. 229/229 tests; verified in-game via headless rig (models, scale vs player, and live skeleton animation all confirmed).
- **Verdant Maw native pack (2026-07-24):** the jungle re-assetted end to end (portal untouched) — 14 Rodin text-to-3D generations, 0 re-rolls. Environment: canopy + banyan trees (baked envelope outline hulls, generated ground-plates surgically removed), giant fern clusters, carnivorous maw plants, a moss-grown idol head landmark, moss boulders, glow shrooms; zone builder is now ground+portal only, all dressing via ZoneAssets, new `verdantMaw` ZONE_AMBIENCE (humid green). Resource nodes: first-ever models for timber / resin / silica / carbon_biomass (log stack, amber-oozing stump, crystal spray, sprouting biomass mound) — these upgrade every zone spawning those types. Creatures: **Vineclaw** (vine-wrapped panther, burst striker), **Sporeback** (mushroom-shelf toad, windup tank), **Bloomfang** (flower-headed stalker, FP drain) — rigged via `Assets/3D/VerdantMaw/` scripts (three armature plans incl. a diagonal 'stalker' layout; smooth 1/d⁴ 3-bone binding since bone-heat fails on Rodin soup; per-creature-named Idle/Walk clips exported in NLA_TRACKS mode to keep multi-rig files clean). New T3-native spawn table, drops, codex entries. 229/229 tests; verified in-game via headless rig.
- **Frozen Tundra re-theme + first rigged creatures (2026-07-24):** the zone re-imagined in the ATLA/Ghibli direction (portal untouched) AND the game's first three fully rigged, animated creatures. New native pack replaces dunkraza/hardlizzy spawns: **Frostfang** (arctic fox-wolf, fast burst striker, new `frostbite` status effect), **Glacierback** (yak-turtle ice-shell tank), **Blubberfin** (belly-sliding penguin-seal, FP drain) — each a Rodin body rigged in Blender via `Assets/3D/FrozenTundra/rig_creatures.py` (procedural armature from mesh-derived landmarks, nearest-segment skinning, parametric Idle/Walk clips on NLA tracks; Enemy.js's existing mixer crossfades them while patrolling). The Cryo Monarch boss got its own model (`Boss_CryoMonarch.glb`, ice-crowned stalking lion) replacing shared Pirate Lizard. Environment: snow pines ×2 variants with baked envelope outline hulls (`hull_pines.py` — 14-ray escape vote + face-aware lift), gnarled dead trees (Rodin pedestal buried below ground at export), ice crystals, snow boulders, frozen shrine well, upgraded frozen lake, arctic `ZONE_AMBIENCE`; titanium/tungsten/silver resource nodes now have GLB models. New codex entries + drop tables. 229/229 tests; verified in-game (animations confirmed live). An aurora was tried and cut — additive sky ribbons read as painted ground stripes at the fixed ortho camera.
- **Spaceship interior re-theme (2026-07-24):** the whole zone re-imagined in the ATLA/Ghibli direction (portals untouched). Hand-built hull shell `models/SpaceshipShell.glb` (wood plank deck + brass inlay, cream/teal paneled walls, portholes, glowing Workspace hatch, low south rim so the camera never clips, framed wall screens replacing the old floating holo panels; source `Assets/3D/SpaceshipInterior/` — `build_shell.py` regenerates) plus 12 Rodin text-to-3D assets: 9 station bodies (workbench fabricator, offload console, charging pod, radar drone-desk, ascension obelisk, amber mastery cabinet, wing-chun sparring dummy, training console, 4× holodeck pylons) and 3 dressing props (potted plant, crate stack, wall pipe manifold). New wiring: `_registerStationModel` late-attach in Environment.js (procedural bodies remain fallbacks), `spaceship` ZONE_AMBIENCE preset (warm cabin), ZoneAssets rework. Mastery took 3 generations (Rodin can't do "screen with content" or interior shelves — final is a clean amber panel cabinet); fabricator/drone/combat re-rolled once for tilted-board fixes. 229/229 tests; verified in-game via headless rig.
- **Asset + boot-fix round (2026-07-23):** 8 new Rodin-generated props (hyper3d.ai text-to-3D via blender-mcp free-trial key) — grounded scout ship at the Landing Site, Ghibli_Tree_D, Mossy_Boulder, plus the five formerly-404 slots (Rock_Cluster, Watchtower, Supply_Crate, Fuel_Barrel, Cyborg_PC) now real files. Blender source: `Assets/3D/LandingProps/LandingProps.blend`. Fixed: sticky sessionStorage model-failure cache (portals could vanish forever in a tab), enemies/nodes now rebuild in place when their GLB finishes loading (no more procedural bodies on boot), portals show a teal fallback ring while their model loads, inverted-hull outlines floored at 0.045 world units so small props (nodes, rocks) aren't outline-less. Round 2: boot overlay gates first paint until GLBs parse (no fallback flash), Rodin props re-shaded to toon in `cloneModel` (no PBR shine) + auto-hulled, lump tree replaced by an oak generated from the owner's reference image (image-to-3D). Round 3: old trees A–C retired; baked flipped-normal outline shells replace runtime hulls on trees (runtime hulls speckle on organic meshes). Round 4: camera pitch 69°→46° (`CAMERA_OFFSET`) so the world reads in profile; forest rebuilt from the owner's three reference images (`Ghibli_Tree_D/H/I/J` = oak/broadleaf/windswept/spruce, weighted pool; E–G retired same-day). Rounds 5–9: tree outlines rebuilt — TreeH now uses a strictly-containing low-poly envelope hull (`Assets/3D/LandingProps/hull_envelope.py`: bold continuous band, interior artifacts geometrically impossible from any angle), the multi-lobed trees (I/J/D) keep err-inward smooth cages with per-tree ink weight (`hull_rebake.py`); TreeI's six free-floating canopy islands bridged to the trunk with real modeled branches (the Rodin mesh shipped as 12 disconnected pieces); `Ghibli_Tree_H2.glb` = cavity-ink variant (crevice depth painted into the texture from the distance-to-envelope field, `cavity_ink.py`) placed at the Landing Site path tree for owner A/B — adopt/tune/revert pending.
- **Reference-integration build** — phases A–E shipped (saves v10→v14). E (2026-07-20): Compute allocation board in ALLOC (Al's attention pool; 0 units = system paused, extras boost output), Al modules (Key Tracker / Overflow Routing / Farm Director / Foreman), factory input hoppers + processing-queue persistence, drone mission queues, stocked-offline rework (full rate when stocked, DORMANT rows when not; 12 h base buffer). Build calls + deferrals (output buffers, storage caps, Triage) in DESIGN-DECISIONS.md. 229/229 tests at last run.
- **Next:** Phase F — skill web with use-XP, stat-purchase retirement (`Plans/Integration-Design.md` §6/§13, save v15). Riskiest diff of the plan — touches every interaction site.
- **Git:** flat policy — commit rounds directly to `main`, push both remotes (see the git paragraph above). The PR-per-feature-branch flow is retired (PR #7 was the last). For anything else about branches, ask git — not this file.

## Folder map

```
index.html            SPA shell — all panel HTML lives here
js/
  main.js             bootstrap, game loop, ALL cross-system wiring (callbacks)
  config.js           every tunable constant
  systems/            41 gameplay systems, one class per file (save, PP, combat,
                      crafting, prestige, ladder, chapters, compute…) — CLAUDE.md's
                      key-files table maps concern → file
  scene/              3D world: Environment.js (all zone building), zones/ (per-zone
                      builders + Mine generator + ComputerBuilding/ shell·mask·interior),
                      ToonMaterials.js, SceneManager.js
  entities/           Player, Enemy (archetypes + bosses), ResourceNode, EntityManager
  ui/                 HUD.js (every panel) + CombatUI, SkillsMenu, portraits
  input/ fx/ util/    touch/keys · particles/popups · NumberFormat
  sync/               SyncClient — localStorage queue → optional server
  vendor/             Three.js (importmap target; no build step)
css/                  stylesheets
models/              196 runtime .glb (player, creatures, bosses, portal, MineKit, Lab + Forge kits, props)
Assets/               source art: 3D/ (Blender sources), Inventory/ (1024px icon art +
                      icons/ 128px served), Video/ (training chamber), SVG/, fonts/
server/               OPTIONAL Express+Postgres sync API: db/migrations/, definitions/
                      (seed data = content defs), repositories/, services/
tests/                npm test → runAll: wiring enforcers (zoneWiring, panelWiring),
                      mine generator/layout, systems/, ui/, server/, sync/
Sessions/             test saves: Endgame_Test.json (god save — feature testing ONLY),
                      Midgame_Test.json
Plans/                design work — see doc index below
docs/                 for-future-claude.md + superpowers/ (spec-era plans & specs)
start-node.bat        serve :8080 (required — file:// is blocked)
start-mobile.bat      LAN serve for phone + QR; append ?debug for on-device console
```

Root oddballs: `combine-js-to-md.ps1` regenerates the `*_compact.txt` / `all-in-one.txt` source dumps (for pasting into LLMs); `codebase-explainer.html` is an older visual codebase map (unmaintained); `New Text Document.txt` is empty; `Telemetry.txt` is a scratch log.

## Doc index

**Live — trust these**
- `CLAUDE.md` — architecture, wiring checklists, gotchas (auto-loaded every session)
- `Plans/Integration-Design.md` — **build order of record**: 7 phases + formulas
- `Plans/DESIGN-DECISIONS.md` — every design decision and veto, logged
- `Plans/Game-Brief.md` — design-language snapshot of the whole game for ideation/chat sessions (no code); pairs with DESIGN-DECISIONS
- `Plans/Expanded-Biome-Worlds-Design.md` — approved design: four 100×100 biomes, route grammar, authored layout data (**not on `main`** — lives only in the archived `backup/machine-phase1-2` branch; the owner shelved that direction 2026-07-31)
- `Plans/Expanded-Biome-Worlds-Implementation.md` — its build order + code findings (**same archived branch**)
- `Plans/ProcessingPower-Systems-Inventory.md` — code-level inventory of every system: formulas + wiring health
- `Plans/Generation-Engine-Design.md` + `Plans/Generation-Engine-Site-Addendum.md` — the evolving-computer design (24 gens, 5 eras) + the owner's site/construction calls (**addendum v3 wins conflicts**); Round 1 (Era 1) shipped 2026-08-02, see Where the work stands
- `Plans/Generation-Engine-Kickoff.md` — the Round-1 coding-session brief (executed; keep for Round-2 pattern)

**Stable reference**
- `Plans/*-Design-Reference.md` — six studied games (NGU, FAPI, Idle Spiral, RuneScape, TPT2, Crashlands) that feed Integration-Design
- `docs/for-future-claude.md` — April 2026; gotcha and working-style sections still good, state sections stale

**Historical — do NOT trust for current state**
`Plans/7-21-to-do` (the Phase E kickoff prompt — executed and shipped 2026-07-20) · `Plans/NEXT-SESSION-KICKOFF.md` (mission complete) · `Plans/game_analysis.md` (superseded by Systems-Inventory) · `Direction.md` · `implementation_plan.md` · `TRIPARTITE_SESSION_WRITEUP.md` · `docs/superpowers/specs/2026-07-07-ngu-feel-roadmap-design.md` (superseded by Integration-Design) · `Plans/*.txt` brainstorms · `July3Plan.docx`
