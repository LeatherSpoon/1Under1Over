# Dependencies
node_modules/

# Git worktrees (isolated feature workspaces)
.worktrees/

# Session/tool state and logs
.superpowers/
.claude/settings.local.json
*.log

# Art sources — the 1024px item originals (~41MB) and their pre-cutout versions
# (~51MB) are now TRACKED (owner call, 2026-07-28): they exist nowhere else in
# the repo, so a clone without them cannot regenerate the 128px derivatives the
# game serves from Assets/Inventory/icons/ (see "Item icon art" in CLAUDE.md).
# They were previously ignored with "keep the originals backed up outside git".

# OS junk
Thumbs.db
Desktop.ini
.DS_Store
*.blend1

# Python bytecode from Blender build-script imports
__pycache__/
